apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: cloud-demo
spec:
  selector:
    app: gateway-service
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
